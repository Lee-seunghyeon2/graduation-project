# 졸업작품 중간보고서 자료 정리 — korean-prompt-improver-mcp (v0)

> 본 문서는 프로젝트 전체 소스코드, 자동 테스트(`test/`), 평가 스크립트(`scripts/`), 평가 결과(`evaluation-results/`), 문서(`docs/`)를 직접 분석해 **실제 구현된 내용만** 정리한 것이다.
> 자동 테스트는 `npx vitest run`으로 실제 실행해 31개 전부 통과를 확인했고, 평가 수치는 `evaluation-results/`의 JSON·MD 원본 기준이다.
> 코드에 없는 기능은 기재하지 않았으며, 불확실한 항목은 "확인 필요"로 표시했다.

---

# PART 1. 프로젝트 자료 정리 (중간보고서 본론용)

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | `korean-prompt-improver-mcp` (package.json). MCP 서버 등록명은 `korean-prompt-improver`, 버전 `0.1.0` |
| package.json description | "v0: 한국어 프롬프트 개선 MCP - 모호성 탐지 및 문맥 기반 해결" |
| 해결하려는 문제 | 한국어 사용자가 LLM에게 주는 요청이 짧고 모호함("이거 정리해줘", "그 기능 빼줘", "전처럼", "조금만 더"). 대명사·성분 생략·정도·비교·범위 표현 때문에 LLM이 잘못된 대상/범위로 작업하거나, 반대로 매번 되묻는 문제 |
| 접근 방식 | 요청을 바로 실행하지 않고 (1) 모호성 유형을 탐지 → (2) 이전 대화·첨부 문서 문맥으로 해석 후보를 찾고 → (3) 결정론적 규칙으로 "문맥 해결 / AI 추론 허용 / 사용자에게 질문" 을 분기 → (4) 모호성이 제거된 "개선된 프롬프트"를 생성 |
| 주요 사용자 | 코드상 명시된 두 Task(`planning`, `writing`) 대상 사용자 = 프로젝트/학업/업무 **계획 수립**을 요청하는 사용자, 보고서·자기소개서·기획서 등 **자료 기반 문서 작성/수정**을 요청하는 사용자. MCP 클라이언트(Claude Desktop 등)를 쓰는 최종 사용자 |
| 입력 | `analyze_prompt` 툴의 인자: `current_user_request`(필수, string), `recent_conversation`(string[]), `document_context`(string), `task_hint`("planning"\|"writing"), `previous_analysis`(이전 결과 JSON 문자열), `answers`(질문 id→답변 텍스트) |
| 최종 출력 | JSON. `status: "ready"`면 `improved_prompt`(개선된 프롬프트 문자열) + `analysis`. `status: "clarification_required"`면 `questions`(최대 3개) + `analysis` |
| 기존 LLM 사용 방식과의 차이 | 일반적 사용은 모호한 요청을 LLM이 그대로 받아 즉시 답을 생성(추측이 섞임). 이 시스템은 **LLM 응답 생성 이전 단계**에서 동작하는 전처리기로, 모호성 "탐지"만 LLM에 맡기고 **최종 RESOLVED/SAFE_INFERENCE/CLARIFICATION 분기와 질문 생성·프롬프트 조립은 LLM이 아닌 결정론적 코드**가 담당해 재현성을 확보 |
| 한 문장 정의 | "한국어의 모호한 작업 요청을 이전 대화·첨부 문서 문맥으로 분석해, 꼭 필요할 때만 명확화 질문을 하고 그 외에는 모호성이 제거된 실행용 프롬프트를 생성하는 MCP 서버(v0)" |

## 2. 전체 시스템 구조

### 2.1 처리 순서 (신규 분석 경로: `runFreshAnalysis`, `src/workflow.ts:35`)

1. `analyze_prompt` 툴 호출 수신 (`src/index.ts:45`) — MCP 클라이언트가 stdio로 전달
2. `previous_analysis` + `answers`가 둘 다 있으면 **이어받기 경로**(`continueFromAnswers`), 아니면 신규 분석
3. `buildContextBundle()` — `recent_conversation` 최근 10턴, `document_context` 최대 4000자로 정리 (`src/contextResolver.ts`)
4. `formatContextForPrompt()` — 문맥을 LLM 프롬프트용 단일 텍스트로 포맷
5. Task 판별: `task_hint`가 있으면 그대로 사용, 없으면 `classifyTask()` **(LLM 호출 1)** → `"planning"` 또는 `"writing"`
6. `analyzeAmbiguities()` **(LLM 호출 2)** — 5개 모호성 유형 정의를 시스템 프롬프트로 주고, 표현별 `candidates / context_support / impact / top_candidate`를 JSON으로 받음
7. `decideAll()` — 각 finding에 `decision`(RESOLVED/SAFE_INFERENCE/CLARIFICATION_REQUIRED) + `resolved_value`를 **결정론적으로** 부여 (`src/decisionEngine.ts`)
8. `generateClarificationQuestions()` — CLARIFICATION_REQUIRED 항목을 우선순위 정렬 후 상위 3개만 질문 템플릿으로 생성 (`src/clarificationQuestionGenerator.ts`)
9. 질문이 1개 이상이면 → `{ status: "clarification_required", questions, analysis }` 반환
10. 질문이 0개면 → `buildImprovedPrompt()`로 최종 프롬프트 조립 → `{ status: "ready", improved_prompt, analysis }` 반환
11. `src/index.ts:94`에서 결과를 `JSON.stringify(result, null, 2)` 텍스트로 MCP 응답

### 2.2 이어받기 경로 (`continueFromAnswers`, `src/workflow.ts:63`) — LLM 재호출 없음

- 이전 `analysis.ambiguities`를 순회하며 `decision === "CLARIFICATION_REQUIRED"` 이고 `question_id`에 대응하는 답변이 있으면 → `decision: "RESOLVED"`, `resolved_value: answers[question_id]`로 전환
- 다시 `generateClarificationQuestions()` → 남은 질문 있으면 재질문, 없으면 `buildImprovedPrompt()`

### 2.3 모듈·파일별 역할

| 파일 | 역할 | LLM 사용 |
|---|---|---|
| `src/index.ts` | MCP 서버 생성(`McpServer`) + `StdioServerTransport` 연결, `analyze_prompt` 툴 1개 등록, zod 입력 스키마 정의, `LLM_PROVIDER` env로 클라이언트 선택 | — |
| `src/workflow.ts` | 전체 오케스트레이션. 신규 분석 / 이어받기 분기, 각 모듈 호출 순서 제어 | 간접 |
| `src/taskClassifier.ts` | 요청+문맥을 `planning`/`writing`으로 분류 | O 호출 |
| `src/ambiguityAnalyzer.ts` | 5개 모호성 유형 정의를 시스템 프롬프트로 제공, 표현별 원시 판단(candidates/context_support/impact/top_candidate) 산출, zod로 검증 | O 호출 |
| `src/decisionEngine.ts` | 순수 함수. impact와 후보 개수, context_support로 RESOLVED/SAFE_INFERENCE/CLARIFICATION_REQUIRED 결정 | X |
| `src/clarificationQuestionGenerator.ts` | 템플릿 기반 질문 생성. MAX 3개, 유형 우선순위 정렬, 후보 2개 이상이면 선택형 질문 | X |
| `src/contextResolver.ts` | recent_conversation(최근 10턴)·document_context(4000자) 정리 및 프롬프트용 텍스트 포맷 | X |
| `src/promptBuilder/index.ts` | task별 빌더 디스패치 | X |
| `src/promptBuilder/common.ts` | 원본 요청에서 모호 표현을 `resolved_value`로 문자열 치환(긴 표현 우선), "표현→확정값" 목록 생성 | X |
| `src/promptBuilder/planningPromptBuilder.ts` | planning용 최종 프롬프트 템플릿 | X |
| `src/promptBuilder/writingPromptBuilder.ts` | writing용 최종 프롬프트 템플릿 | X |
| `src/llm/client.ts` | `LLMClient` 인터페이스, `AnthropicLLMClient`(기본, `claude-sonnet-4-5`, max_tokens 1500), `parseJsonResponse()`(코드펜스·`<think>` 블록 방어 파싱) | O |
| `src/llm/geminiClient.ts` | Gemini REST(`v1beta ... :generateContent`) 호출, 기본 모델 `gemini-2.0-flash`, `GEMINI_MODEL` env로 오버라이드, temperature 0.2 | O |
| `src/llm/groqClient.ts` | Groq(OpenAI 호환 chat completions) 호출, 기본 모델 `llama-3.1-8b-instant`, `GROQ_MODEL` env 오버라이드, temperature 0.2, **max_tokens 미설정** | O |
| `src/types.ts` | 전 모듈 공유 타입 정의 | — |

### 2.4 MCP Client / MCP Server / LLM / API 관계

```
[MCP Client: Claude Desktop / MCP Inspector]
        |  stdio (JSON-RPC), tool call: analyze_prompt
        v
[MCP Server: 본 프로젝트 dist/index.js]
   McpServer + StdioServerTransport
        |  runWorkflow()
        v
   taskClassifier / ambiguityAnalyzer
        |  LLMClient.complete(system, user)
        v
[LLM API: Anthropic SDK  또는  Gemini REST  또는  Groq REST]  <- 외부 HTTPS
```

- MCP 서버가 **동시에 LLM API 클라이언트**다. 신규 분석 1건당 LLM 호출 최대 2회(task 분류 + 모호성 분석), `task_hint` 제공 시 1회.
- DecisionEngine·QuestionGenerator·PromptBuilder·ContextResolver는 LLM을 전혀 호출하지 않는다.

### 2.5 Gemini와 Groq가 쓰이는 위치

- 둘 다 `LLMClient` 인터페이스의 교체 가능한 구현체이며, `LLM_PROVIDER` 환경변수(`anthropic`|`gemini`|`groq`)로 **런타임에 하나만** 선택된다 (`src/index.ts:18`). 특정 하위 기능에 각기 배정된 것이 아니라, 선택된 provider가 파이프라인 전체(task 분류 + 모호성 분석)를 담당한다.
- 실제 사용 기록:
  - Gemini: 2026-08-13 수동 실사용 검증(`gemini-flash-latest`), 2026-08-18 30케이스 평가(`gemini-flash-latest`)
  - Groq: 2026-08-19 30케이스 평가(`qwen/qwen3.6-27b`)
  - Anthropic: 코드상 기본값이나 `.env`에 `ANTHROPIC_API_KEY` 없음 → **실제 실행 기록 확인 안 됨 (확인 필요)**

### 2.6 외부 API key 사용 위치

- `process.loadEnvFile()`로 `.env` 로드 (`src/index.ts:4`, 평가 스크립트도 동일)
- `ANTHROPIC_API_KEY` → `src/llm/client.ts:24` (AnthropicLLMClient)
- `GEMINI_API_KEY` → `src/llm/geminiClient.ts:17`, URL 쿼리 파라미터로 전달
- `GROQ_API_KEY` → `src/llm/groqClient.ts:19`, `Authorization: Bearer` 헤더
- 현재 `.env` 키 목록: `LLM_PROVIDER`, `GEMINI_API_KEY`, `GEMINI_MODEL`, `GROQ_API_KEY`, `GROQ_MODEL` (값은 비공개/미확인)

### 2.7 상태값·JSON 구조 (`src/types.ts`)

```ts
Task            = "planning" | "writing"
AmbiguityType   = "reference" | "omission" | "degree" | "comparison" | "scope"
ContextSupport  = "strong" | "medium" | "weak"
Impact          = "high" | "low"
Decision        = "RESOLVED" | "SAFE_INFERENCE" | "CLARIFICATION_REQUIRED"

RawAmbiguityFinding { type, expression, candidates[], context_support, impact, top_candidate? }
AmbiguityFinding    extends Raw + { decision, resolved_value, question_id? }
ClarificationQuestion { id, type, question, options? }
AnalysisResult      { task, original_request, ambiguities[], questions[] }
WorkflowOutput =
  | { status: "clarification_required", questions: ClarificationQuestion[], analysis }
  | { status: "ready",                  improved_prompt: string,           analysis }
```

- **주의**: workflow 최상위 `status`는 `ready` / `clarification_required` 두 개뿐이다. `RESOLVED`/`SAFE_INFERENCE`/`CLARIFICATION_REQUIRED`는 개별 모호 표현(finding) 단위의 `decision` 값이다.

### 2.8 전체 데이터 흐름(단계별)

`current_user_request(+대화+문서)` → ContextBundle → (LLM) Task → (LLM) `RawAmbiguityFinding[]` → (규칙) `AmbiguityFinding[]`(decision 부여) → (템플릿) `ClarificationQuestion[]` → 질문 有: 사용자에게 반환 / 질문 無: (템플릿) `improved_prompt` 조립 → 반환. 사용자가 답변 시 `previous_analysis`+`answers` 재입력 → 해당 finding을 RESOLVED로 전환 → improved_prompt 조립.

## 3. 모호성 분석 방식

### 3.1 구현된 5개 유형 (`src/ambiguityAnalyzer.ts` 시스템 프롬프트에 정의)

| 유형 | 정의(프롬프트 원문 기준) | 대표 예시 |
|---|---|---|
| `reference` (지시 대상 모호성) | "이거", "그거", "이 부분", "그 기능", "위에 거" 등 지시어 | "그 기능 빼줘" |
| `omission` (성분 생략) | "해줘", "바꿔줘", "빼줘", "정리해줘" 등 대상/목적이 생략된 표현 | "정리해줘.", "바꿔줘." |
| `degree` (정도 모호성) | "좀", "많이", "적당히", "조금 더", "길게", "짧게" 등 | "조금 더 길게 써줘" |
| `comparison` (비교 기준 모호성) | "전처럼", "비슷하게", "아까처럼", "이거랑 비슷하게" 등 | "전처럼 써줘" |
| `scope` (범위 모호성) | "여기만", "이 부분만", "일부만", "전체적으로", "필요한 부분만" 등 | "일부만 수정해줘" |

### 3.2 판별 방법 — 규칙 + LLM 하이브리드

- **LLM 담당 (구조적 판단)**: 위 5개 유형 정의를 시스템 프롬프트로 주고, 형태소 분석기·별도 NLP 모델 없이 LLM이 요청에서 모호 표현을 찾아 표현별로 산출:
  - `expression`(원문 그대로), `candidates`(문맥에서 찾은 해석 후보, 없으면 `[]`), `context_support`(strong/medium/weak), `impact`(high/low), `top_candidate`
  - degree 특칙: 사용자가 글자수/분량 준수를 강조했으나 실제 기준값이 문맥에 없으면 `candidates=[]`, `impact="high"`
  - 응답은 반드시 지정 JSON 형식, `zod`(`ResponseSchema`)로 검증
- **규칙 담당 (결정론적 판정)**: `decisionEngine.ts`가 LLM 산출값을 받아 최종 decision 부여 (아래 5장). LLM을 재호출하지 않아 테스트 재현성 확보 ("LLM이 매번 다르게 판단해 테스트가 흔들리는 것 방지" — 코드 주석)

### 3.3 모호하지 않은 입력의 판정

- AmbiguityAnalyzer 프롬프트: "모호성이 전혀 없는 명확한 요청이면 빈 배열을 반환하라" → `ambiguities: []`
- `decideAll([])` → 빈 배열 → `generateClarificationQuestions`가 질문 0개 → 곧바로 `status: "ready"`로 improved_prompt 생성 (원본 요청이 거의 그대로 `[명시화된 요청]`에 들어감)

## 4. 질문 생성 방식 (`src/clarificationQuestionGenerator.ts`)

- **언제 질문하나**: `decision === "CLARIFICATION_REQUIRED"`인 finding이 1개 이상일 때만. (해당 조건은 5장)
- **개수 제한**: `MAX_QUESTIONS = 3`. 초과분은 `question_id` 없이 `CLARIFICATION_REQUIRED` 상태로 남는다 (다음 턴 자동 이월 정책은 미구현 — v1-backlog #3).
- **우선순위**(`TYPE_PRIORITY`): `omission`(1) > `reference`(2) = `scope`(2) > `comparison`(3) > `degree`(4). 이 순으로 정렬 후 상위 3개 선택, `q1`, `q2`, `q3` 부여.
- **질문 형태**: 후보(candidates)가 2개 이상이면 **선택형** — `"{표현}"은(는) 다음 중 무엇을 의미하나요?\n1. ...\n2. ...` + `options` 배열. 후보가 0개면 자유입력형 — degree는 `"...에 대해 지켜야 할 구체적인 기준이 있다면 알려주세요. (예: 글자 수, 분량, 페이지 수 등)"`, 그 외는 `"...이(가) 정확히 무엇을 가리키는지 알려주세요."`
- **LLM 프롬프트**: 질문 생성 단계는 **LLM을 쓰지 않는다**. 전부 위 문자열 템플릿(결정론적). 별도 질문 생성 프롬프트가 없다.
- **답변 결합** (`continueFromAnswers`): 호출자가 이전 `analysis` JSON 전체 + `answers: {q1: "사용자 답변텍스트", ...}`를 다시 넘기면, 해당 `question_id`를 가진 finding의 `decision`을 `RESOLVED`로, `resolved_value`를 답변 텍스트로 설정. `question_id`는 유지(추적용). 이후 PromptBuilder가 원본 요청 문자열에서 그 표현을 답변값으로 치환. 세션 DB 없이 무상태로 동작.

## 5. 결과 라우팅 (`src/decisionEngine.ts` `decide()`)

입력: `impact`, 중복 제거한 후보 개수 `unique.length`, `context_support`. 순서대로 평가한다:

| 조건 | 결정 | resolved_value |
|---|---|---|
| `impact === "high"` **AND** `unique.length !== 1` (후보 0개 또는 2개 이상) | **CLARIFICATION_REQUIRED** | `null` |
| 위 아님 **AND** `unique.length === 1` **AND** `context_support !== "weak"` | **RESOLVED** | 유일 후보 |
| 그 외 전부 (impact low거나, 후보 1개인데 근거 weak 등) | **SAFE_INFERENCE** | `top_candidate` → 없으면 유일 후보 → 없으면 `null` |

- 핵심 규칙(코드 주석): "질문이 필요한 조건 = 유력 후보가 2개 이상 남음 AND 후보 선택에 따라 결과가 크게 달라짐(impact=high)"
- 예외: 후보 0개 + impact high(예: 기준값 없는 degree "분량 꼭 지켜줘") → 안전 추론 근거 자체가 없으므로 CLARIFICATION_REQUIRED
- **workflow 최상위 status는 이 decision들의 집계 결과**: CLARIFICATION_REQUIRED finding이 하나라도 질문으로 선택되면 `status: "clarification_required"`, 아니면 `status: "ready"`.
- 이외 상태는 없다. 오류 시(LLM 파싱 실패, zod 위반 등)는 예외가 던져져 MCP 툴 호출 자체가 에러로 종료된다(평가 스크립트에서는 `ERROR`로 분류).

## 6. 최종 프롬프트 생성 (`src/promptBuilder/`)

### 6.1 조립 방식 (전부 템플릿, LLM 미사용)

1. `buildExplicitRequest()`: 원본 요청 문자열에서 `resolved_value`가 있는 모호 표현을 그 값으로 문자열 치환. 겹침 오치환 방지를 위해 표현 길이 내림차순 처리.
2. `buildResolvedInfoList()`: 해결된 항목을 `- "표현" -> 확정값 (근거)` 불릿 목록으로. 근거 = 문맥상 확정(RESOLVED) / AI 추론(영향 낮음)(SAFE_INFERENCE) / 사용자 답변으로 확정(이어받기).
3. task별 템플릿으로 감싼다.

### 6.2 최종 프롬프트 구성 요소

- 첫 줄: task별 지시문 ("다음 요청에 대한 실행 계획을 작성한다." / "다음 요청에 따라 문서를 작성/수정한다.")
- `[명시화된 요청]` : 치환된 요청 문장
- `[확정된 정보]` : 표현→확정값 목록 (없으면 "(명시적으로 해결된 모호 표현 없음)")
- `[첨부 문서 반영 지침]` / `[첨부 자료 반영 지침]` : `document_context`가 있을 때만
- `[출력 형식 지침]` : task별 고정 문구

### 6.3 원본 대비 추가되는 정보

- 대명사/생략어가 **구체적 대상·기준·범위 값으로 치환**됨
- 어떤 표현이 무엇으로 확정됐고 그 근거가 문맥인지 AI 추론인지 사용자 답변인지 **투명하게 명시**
- task 종류, 첨부 문서 반영 지침, 출력 형식 지침

### 6.4 실제 예제 1 (2026-08-13 수동 검증 Case B→C, writing)

| 단계 | 내용 |
|---|---|
| 원본 요청 | `"그 기능 빼줘."` + recent_conversation: `["자동 저장 기능을 추가했어.", "알림 기능도 넣었어."]` |
| 분석 결과 | `reference` "그 기능", candidates=`["자동 저장 기능","알림 기능"]`, context_support=medium, impact=high → **CLARIFICATION_REQUIRED** |
| 질문 | q1(선택형): `"그 기능"은(는) 다음 중 무엇을 의미하나요?\n1. 자동 저장 기능\n2. 알림 기능` |
| 사용자 답변 | `answers: {"q1": "자동 저장 기능"}` (+ previous_analysis 재전달) |
| 최종 프롬프트 | 아래 |

```
다음 요청에 따라 문서를 작성/수정한다.

[명시화된 요청]
자동 저장 기능 빼줘.

[확정된 정보]
- "그 기능" -> 자동 저장 기능 (사용자 답변으로 확정)

[출력 형식 지침]
위에서 확정된 대상/범위/기준/스타일을 그대로 반영하여 완성도 있는 문서를 작성한다.
```

### 6.5 실제 예제 2 (2026-08-18 평가 R1, planning, 질문 없이 RESOLVED — 평가 로그 원문)

- 원본: `"이거 보고 준비 계획 짜줘."` + document_context: `"파일명: AI 아이디어 공모전 안내문 ..."`
- 분석: `reference` "이거" → candidates `["AI 아이디어 공모전 안내문"]`, RESOLVED
- 질문: 없음
- 최종 프롬프트:

```
다음 요청에 대한 실행 계획을 작성한다.

[명시화된 요청]
AI 아이디어 공모전 안내문 보고 준비 계획 짜줘.

[확정된 정보]
- "이거" -> AI 아이디어 공모전 안내문 (문맥상 확정)

[첨부 문서 반영 지침]
첨부된 문서에 명시된 마감일, 제출물, 평가 기준, 요구사항이 있다면 반드시 계획에 반영한다.

[출력 형식 지침]
구체적인 단계별 실행 계획(할 일, 순서, 기한 고려사항 포함)으로 작성한다.
```

> v1-backlog #4: 현재 improved_prompt는 결정론적 "문자열 치환 템플릿"이라 한 문단으로 자연스럽게 재작성되지는 않는다(선택적 LLM 재작성 단계는 미구현).

## 7. 기술 스택

| 기술 | 버전(package.json) | 시스템에서의 역할 |
|---|---|---|
| TypeScript | `^5.7.3` (devDep) | 전체 구현 언어. `strict: true`, target ES2022, module NodeNext (tsconfig.json) |
| Node.js | `@types/node ^22.10.5`, `process.loadEnvFile()` 지원 버전(Node 20.6+) 필요 | 런타임. 내장 `fetch`로 Gemini/Groq REST 호출(별도 HTTP 라이브러리 없음), 내장 `.env` 로더 사용 |
| MCP SDK | `@modelcontextprotocol/sdk ^1.30.0` | `McpServer`, `StdioServerTransport`, 툴 등록. MCP 클라이언트(Claude Desktop 등)와 stdio JSON-RPC 통신 |
| MCP Inspector | `@modelcontextprotocol/inspector ^2.2.0` (devDep) | 로컬 툴 수동 점검용 (`.claude/launch.json`에 `mcp-inspector` 실행 구성, port 6274) |
| Zod | `zod ^3.24.1` | (1) `analyze_prompt` 입력 스키마 정의, (2) AmbiguityAnalyzer의 LLM JSON 응답 런타임 검증(`RawFindingSchema`, `ResponseSchema`) |
| Anthropic SDK | `@anthropic-ai/sdk ^0.65.0` | `AnthropicLLMClient` 구현. 기본 모델 `claude-sonnet-4-5`, `max_tokens: 1500`. (기본 provider지만 실행 기록 미확인) |
| Gemini API | SDK 없음, REST 직접 호출 | `GeminiLLMClient`. `generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`, 기본 `gemini-2.0-flash`, temperature 0.2. 무료 티어로 실사용 검증에 사용 |
| Groq API | SDK 없음, REST 직접 호출 | `GroqLLMClient`. OpenAI 호환 `api.groq.com/openai/v1/chat/completions`, 기본 `llama-3.1-8b-instant`, temperature 0.2. 2026-08-19 평가에 `qwen/qwen3.6-27b` 사용 |
| Vitest | `vitest ^2.1.8` (devDep) | 자동 테스트 러너 (`npm test` = `vitest run`) |
| tsx | `tsx ^4.19.2` (devDep) | `dev` 스크립트 및 평가/수동 스크립트 실행(`node --import tsx`) |

- 빌드: `tsc -p tsconfig.json` → `dist/`. 실행: `node dist/index.js`.
- **미사용 확인**: 형태소 분석기, 벡터DB/임베딩/RAG, 별도 웹 서버·프레임워크, 세션 DB — 전부 없음.

## 8. 테스트 및 검증

### 8.1 자동 테스트 (vitest, `test/`) — 실행 확인: 31개 전부 PASS

| 파일 | 테스트 수 | 대상 |
|---|---|---|
| `test/decisionEngine.test.ts` | 7 | `decide()` 경계값: 후보 개수 x impact x context_support 조합, 중복 후보 처리 |
| `test/workflow.test.ts` | 16 | 5개 유형 각각 A(RESOLVED)/B(SAFE_INFERENCE)/C(CLARIFICATION_REQUIRED) 시나리오 + 답변 이어받기 1건 |
| `test/llmClient.test.ts` | 4 | `parseJsonResponse()`: 순수 JSON / 코드펜스 / `<think>` 블록 / 둘 다 |
| `test/taskClassifier.test.ts` | 2 | planning/writing 분류 |
| `test/promptBuilder.test.ts` | 2 | planning·writing 최종 프롬프트에 확정 정보 반영, task별 지침 차이 |
| **합계** | **31** | |

- **테스트 방식**: `StubLLMClient`(`test/stubLlmClient.ts`) — 미리 준비한 JSON 문자열을 호출 순서대로 반환. 네트워크 호출 없음. DecisionEngine·QuestionGenerator·PromptBuilder는 순수 함수라 stub도 불필요.
- **stub/mock LLM 사용**: 예 (자동 테스트 전부)
- **실제 Gemini API 테스트**: 자동 테스트에는 없음. 별도 "evaluation runner"(`scripts/evaluate-v0-additional.ts`)와 수동 스크립트(`scripts/manual-test.ts`)에서 실제 호출.
- **Claude Desktop MCP 연동 테스트**: 예 — `docs/v0-manual-verification.md`(2026-08-13)에 `claude_desktop_config.json`에 stdio 서버 등록 후 Case A/B/C 3건 검증 기록. (이후 재검증 기록은 없음)

### 8.2 실제 확인된 성공 사례 (기록 기준)

- 2026-08-13 수동 검증 3건 전부 성공: Case A(reference RESOLVED + degree SAFE_INFERENCE, 질문 없음), Case B(reference 후보 2개 → 질문), Case C(답변 이어받기 → RESOLVED 전환 + improved_prompt 생성). 3개 경로(자동 처리 / 질문 / 이어받기) 모두 실제 LLM·실제 MCP 클라이언트에서 동작 확인.
- 2026-08-18(Gemini) 및 2026-08-19(Groq) 평가에서 공통 PASS: `R1`(첨부문서 reference RESOLVED), `O4`(문맥 없는 omission → 질문), `D3`(강조는 있으나 기준값 없는 degree → 질문). Groq 실행에서 추가 PASS: `CTX2`(두 문서 마감일 충돌 → 질문), `QLIMIT1`(모호성 4종 동시 → 질문 3개 이하), `C2`, `C4`, `S1`, `FN1` 등.

### 8.3 발생했던 오류와 해결

| 오류 | 해결 |
|---|---|
| reasoning 모델이 `<think>...</think>` 사고 과정을 JSON 앞에 붙여 파싱 실패 | `parseJsonResponse()`에 `<think>` 블록 제거 + 코드펜스 추출 로직 추가 (`test/llmClient.test.ts`에 회귀 테스트 4건) |
| 자정(UTC) 경계에서 평가 run이 새 파일로 튀어 진행분 유실 | `resolveRunDate()` — 가장 최근 jsonl 날짜 재사용, `EVAL_FRESH=1`로만 새 파일 |
| Gemini 무료 티어 429/quota로 대량 ERROR | 지수 backoff 재시도(`RETRY_DELAYS_MS`), 케이스별 즉시 JSONL 저장(crash-safe), `--report-only` 재리포트. 이후 provider를 Groq로 전환해 재평가 |

### 8.4 현재 남아 있는 문제 (v1-backlog 기준, 미해결)

1. **[P0] Groq reasoning 모델 응답 절단** — `GroqLLMClient`에 `max_tokens` 미설정. `<think>` 안에서 자기수정을 수천 단어 반복하다 JSON 출력 전 응답이 잘림. 2026-08-19 30케이스 중 ERROR 7건(23%) 중 6건이 이 원인
2. **degree impact 과대평가** — "조금", "조금만 더" 같은 일반 정도부사에도 impact high를 매겨 불필요한 질문 (D2, S3, R2 등 재현)
3. **omission 중복 질문** — 같은 문장에서 reference/scope/comparison이 이미 대상을 확정했는데 동사("작성해줘" 등)에 대해 omission을 또 만들어 재질문 (C3, FP1, FP2, S2)
4. **최근성/출처 우선순위 미반영** — 여러 턴 문맥에서 가장 최근 화제, "이번에는" 같은 override 표현, 문서 vs 최신 발화 충돌 처리 규칙 없음 (R2, CTX1)
5. **zod 스키마 위반 시 재시도 없이 즉시 ERROR** — LLM이 `"impact":"medium"` 반환하면 전체 요청 폐기 (M3)
6. **질문 4개 이상일 때 초과분 이월 정책 미구현**
7. **improved_prompt가 기계적 문자열 치환** — 자연스러운 재작성 없음
8. 평가 하네스 채점이 "type 라벨 정확 일치"를 요구해, 기능은 맞고 라벨만 다른 케이스를 FAIL로 집계 (O2, M4, FN2, S2)

## 9. 현재 구현 완료 범위

### 구현 완료

- `analyze_prompt` 단일 MCP 툴 + stdio 서버
- 3개 LLM provider 교체 구조 (anthropic/gemini/groq), env 기반 선택
- Task 자동 분류(planning/writing) + `task_hint` 수동 지정
- 5개 모호성 유형 LLM 탐지 + zod 검증
- 결정론적 DecisionEngine (RESOLVED/SAFE_INFERENCE/CLARIFICATION_REQUIRED, 3규칙)
- 템플릿 기반 질문 생성 (최대 3개, 유형 우선순위, 선택형/자유입력형)
- 답변 이어받기 (previous_analysis + answers, 무상태)
- task별 최종 프롬프트 조립 (명시화·확정정보·지침)
- 문맥 정리 (대화 10턴, 문서 4000자 제한)
- `<think>`/코드펜스 방어 JSON 파싱
- 자동 테스트 31개 + 30케이스 실LLM 평가 하네스(자동 채점, 실패 8분류, MD/JSON 리포트) + 수동 검증 스크립트

### 부분 구현

- LLM 실사용 검증: Gemini·Groq는 검증됨. Anthropic 경로는 코드만 있고 실행 기록 미확인
- 다중 턴 문맥 활용: 시간순 텍스트로만 전달, 최근성 가중·의미적 연결 판단은 프롬프트에 미반영 (원 스펙 5번 부분 충족)
- 평가: PASS/FAIL 자동 채점은 되나 채점 로직 자체에 결함(라벨 일치 강제), 정량 지표(정확도 등) 미산출
- Claude Desktop 연동: 1회(2026-08-13) 검증 후 재검증 없음

### 미구현

- 질문 4개 이상 시 다음 턴 자동 이월
- improved_prompt의 LLM 재작성 단계
- LLM 응답 스키마 위반 시 self-repair 재시도
- Groq max_tokens / reasoning_format 설정
- planning/writing 외 Task 유형
- 세션 저장소(DB), 서버측 상태 관리
- CI, 정량 평가지표(정확도/질문 정확률/토큰 절감률 등)

## 10. 프로젝트 디렉터리 구조

```
졸업 평가/
├── package.json                          # 프로젝트 메타, deps, scripts(build/start/dev/test)
├── tsconfig.json                         # TS 컴파일 설정 (strict, ES2022, NodeNext, src->dist)
├── .env                                  # LLM_PROVIDER, GEMINI/GROQ API key·모델명 (gitignore됨)
├── .gitignore                            # node_modules, dist, .env, *.log
├── .claude/
│   ├── launch.json                       # MCP Inspector 실행 구성(port 6274)
│   └── settings.local.json               # 로컬 권한 허용 목록
├── src/
│   ├── index.ts                          # MCP 서버 진입점, analyze_prompt 툴 등록, provider 선택
│   ├── workflow.ts                       # 오케스트레이션(신규 분석 / 답변 이어받기)
│   ├── types.ts                          # 공유 타입 (Task, AmbiguityType, Decision, WorkflowOutput 등)
│   ├── taskClassifier.ts                 # (LLM) planning/writing 분류
│   ├── ambiguityAnalyzer.ts              # (LLM) 5개 모호성 유형 탐지 + zod 검증
│   ├── decisionEngine.ts                 # (규칙) RESOLVED/SAFE_INFERENCE/CLARIFICATION_REQUIRED 판정
│   ├── clarificationQuestionGenerator.ts # (템플릿) 질문 생성, 최대 3개, 우선순위 정렬
│   ├── contextResolver.ts                # (규칙) 대화·문서 문맥 정리·포맷
│   └── llm/
│       ├── client.ts                     # LLMClient 인터페이스, AnthropicLLMClient, parseJsonResponse
│       ├── geminiClient.ts               # Gemini REST 클라이언트
│       └── groqClient.ts                 # Groq REST 클라이언트
│   └── promptBuilder/
│       ├── index.ts                      # task별 빌더 디스패치
│       ├── common.ts                     # 모호 표현 치환, 확정 정보 목록화
│       ├── planningPromptBuilder.ts      # planning용 최종 프롬프트 템플릿
│       └── writingPromptBuilder.ts       # writing용 최종 프롬프트 템플릿
├── test/                                 # 자동 테스트 (vitest, stub LLM)
│   ├── stubLlmClient.ts                  # 테스트용 가짜 LLMClient + 응답 헬퍼
│   ├── decisionEngine.test.ts            # 7개: 순수 로직 경계값
│   ├── workflow.test.ts                  # 16개: 5유형 x 3시나리오 + 이어받기
│   ├── llmClient.test.ts                 # 4개: JSON 파싱 방어 로직
│   ├── taskClassifier.test.ts            # 2개
│   └── promptBuilder.test.ts             # 2개
├── tests/evaluation/
│   └── v0-additional-cases.json          # 30케이스 평가 fixture (10개 카테고리)
├── scripts/
│   ├── evaluate-v0-additional.ts         # 실LLM 평가 러너 + 자동 채점 + MD/JSON 리포트
│   └── manual-test.ts                    # 실LLM 수동 3케이스 스크립트
├── evaluation-results/
│   ├── v0-additional-2026-08-18.{json,jsonl,md}          # Gemini(gemini-flash-latest) 평가
│   ├── v0-additional-2026-08-19.{json,jsonl,md}          # Groq(qwen/qwen3.6-27b) 평가
│   └── archive-gemini-attempt2/
│       └── v0-additional-2026-08-19-gemini.{json,jsonl,md}  # Gemini 재시도(quota로 대부분 ERROR)
├── docs/
│   ├── v0-manual-verification.md         # 2026-08-13 Claude Desktop 실연동 검증 기록(Case A/B/C)
│   └── v1-backlog.md                     # v1 개선 후보 12항목
└── dist/                                 # tsc 빌드 산출물 (gitignore됨)
```

---

# PART 2. "구현 및 결과분석" 전용 자료

## 1. 실험 목적

v0(최소 동작 버전)이 세 처리 경로 — (1) 문맥으로 자동 해결(RESOLVED) / (2) AI 추론 허용(SAFE_INFERENCE) / (3) 사용자에게 질문(CLARIFICATION_REQUIRED) — 과 (4) 답변 이어받기 흐름을, stub이 아닌 실제 LLM 및 실제 MCP 클라이언트 위에서 스펙 의도대로 수행하는지 확인. 더불어 5개 모호성 유형별·경계 사례별로 "불필요한 질문 / 필요한 질문 누락" 실패 패턴을 수집해 v1 개선 항목을 도출.

## 2. 실험 환경

- OS: Windows 11, Node.js(내장 `fetch`·`.env` 로더 사용), TypeScript, 로컬 stdio 실행
- 3종 검증 레벨:
  1. 자동 단위 테스트 — vitest, `StubLLMClient`(네트워크 없음)
  2. 실LLM 평가 하네스 — `scripts/evaluate-v0-additional.ts`, 30케이스 fixture, 케이스당 12s 지연 + 429 backoff 재시도, 결과 즉시 JSONL 저장
  3. 실MCP 연동 — Claude Desktop에 `korean-prompt-improver` stdio 서버 등록(2026-08-13)
- 프로덕션 로직(`src/*`)은 평가 시 수정 없이 호출만 함

## 3. 사용 모델

| 일자 | Provider | 모델 | 용도 |
|---|---|---|---|
| 2026-08-13 | Gemini (무료 티어) | `gemini-flash-latest` | 수동 실연동 검증(Claude Desktop) |
| 2026-08-18 | Gemini (무료 티어) | `gemini-flash-latest` | 30케이스 자동 평가 |
| 2026-08-19 | Groq | `qwen/qwen3.6-27b` | 30케이스 자동 평가 |
| 2026-08-19 | Gemini (무료 티어) | `gemini-flash-latest` | 30케이스 재시도(quota로 사실상 실패, archive) |
| (자동 테스트) | 없음 | StubLLMClient | 31개 단위 테스트 |
| (코드 기본값) | Anthropic | `claude-sonnet-4-5` | 실행 기록 확인 안 됨 |

## 4. 테스트 데이터 / 테스트 프롬프트

- 자동 테스트: 코드 내 인라인 시나리오 (5유형 x RESOLVED/SAFE_INFERENCE/CLARIFICATION 각 1 + 이어받기 1)
- 실LLM 평가: `tests/evaluation/v0-additional-cases.json` — **30케이스**, 10개 카테고리:
  reference(R1–R3), omission(O1–O4), degree(D1–D4), comparison(C1–C4), scope(S1–S4), compound(M1–M4), false_positive(FP1–FP2), false_negative(FN1–FN2), context_conflict(CTX1–CTX2), question_limit(QLIMIT1)
- 각 케이스: `input`(current_user_request + 선택적 recent_conversation/document_context) + `expected`(기대 status, 유형, decision, 질문 개수 범위, resolved 키워드, improved_prompt 포함/불포함 문구 등). `review_candidate: true` 3건(R3, M2, S4)은 PASS/FAIL 대신 REVIEW.
- 예시 케이스: `R1` "이거 보고 준비 계획 짜줘."(+공모전 안내문) / `O2` "하나는 빼줘."(+3항목 보고서 구성) / `D3` "분량에 맞게 줄여줘."(+"분량 제한을 반드시 맞춰야 해") / `CTX1` "그 기준으로 작성해줘."(문서 1,000자 vs 발화 700자)

## 5. 평가 기준 (`grade()`, 7개 체크)

- A. status 일치 (ready / clarification_required, 대안 허용) — 불일치 시 Unnecessary/Missing Clarification·Decision Failure로 분류
- B. 기대 모호성 유형 탐지 여부 (`allow_not_detected` 예외)
- C. 유형별 decision 일치 (`decision_match_mode: "any"` 옵션)
- D. 질문 개수 범위 (`question_count_min ~ max`)
- E. `resolved_value`에 기대 키워드 포함 여부
- F. improved_prompt 필수 포함/불포함 문구 (ready일 때)
- G. false_positive / false_negative / unnecessary_question / priority 진단 플래그
- 판정: `review_candidate`면 REVIEW, 아니면 실패 이유 0개 → PASS / 1개 이상 → FAIL. LLM 호출·파싱 예외 → ERROR
- 실패 8분류: Ambiguity Detection / Context Resolution / Impact Judgment / Decision / Unnecessary Clarification / Missing Clarification / Question Generation / Prompt Explicitization Failure

## 6. 테스트 케이스 개수

| 구분 | 개수 |
|---|---|
| 자동 단위 테스트 | 31 |
| 실LLM 평가 fixture | 30 (그 중 REVIEW 대상 3) |
| 실MCP 수동 검증(2026-08-13) | 3 (Case A/B/C) |
| 수동 스크립트(`manual-test.ts`) | 3 |

## 7~9. 성공/실패 개수 및 유형별 결과

### 7.1 자동 단위 테스트

- 31 / 31 PASS (2026-08-29 재실행 확인), FAIL 0

### 7.2 실LLM 평가 — 2026-08-18 (Gemini `gemini-flash-latest`)

전체: PASS 6 / FAIL 8 / REVIEW 1 / ERROR 15 (정상 완료 API 호출 15건, quota 실패 13, 기타 2)

| 카테고리 | PASS | FAIL | REVIEW | ERROR |
|---|---|---|---|---|
| reference | 1 | 1 | 1 | 0 |
| omission | 1 | 3 | 0 | 0 |
| degree | 3 | 1 | 0 | 0 |
| comparison | 1 | 1 | 0 | 2 |
| scope | 0 | 0 | 0 | 4 |
| compound | 0 | 1 | 0 | 3 |
| false_positive | 0 | 1 | 0 | 1 |
| false_negative | 0 | 0 | 0 | 2 |
| context_conflict | 0 | 0 | 0 | 2 |
| question_limit | 0 | 0 | 0 | 1 |

### 7.3 실LLM 평가 — 2026-08-19 (Groq `qwen/qwen3.6-27b`)

전체: PASS 9 / FAIL 12 / REVIEW 2 / ERROR 7 (정상 완료 23건, quota 0, JSON 파싱 실패 6, 기타 1)

| 카테고리 | PASS | FAIL | REVIEW | ERROR |
|---|---|---|---|---|
| reference | 1 | 1 | 1 | 0 |
| omission | 1 | 2 | 0 | 1 |
| degree | 1 | 1 | 0 | 2 |
| comparison | 2 | 1 | 0 | 1 |
| scope | 1 | 2 | 0 | 1 |
| compound | 0 | 1 | 1 | 2 |
| false_positive | 0 | 2 | 0 | 0 |
| false_negative | 1 | 1 | 0 | 0 |
| context_conflict | 1 | 1 | 0 | 0 |
| question_limit | 1 | 0 | 0 | 0 |

케이스별(08-19): R1 PASS / R2 FAIL / R3 REVIEW / O1 ERROR / O2 FAIL / O3 FAIL / O4 PASS / D1 ERROR / D2 FAIL / D3 PASS / D4 ERROR / C1 ERROR / C2 PASS / C3 FAIL / C4 PASS / S1 PASS / S2 FAIL / S3 FAIL / S4 ERROR / M1 ERROR / M2 REVIEW / M3 ERROR / M4 FAIL / FP1 FAIL / FP2 FAIL / FN1 PASS / FN2 FAIL / CTX1 FAIL / CTX2 PASS / QLIMIT1 PASS

### 7.4 실LLM 평가 — 2026-08-19 Gemini 재시도 (archive)

PASS 0 / FAIL 2 / ERROR 28 (quota 실패 24) — 무료 티어 quota 소진으로 유효 데이터 없음. 참고 자료로만 보관.

### 7.5 실MCP 수동 검증 (2026-08-13, Gemini)

Case A/B/C 3건 전부 성공 (자동 처리 / 질문 / 답변 이어받기).

> 주의: FAIL 12건 중 O2·M4·FN2·S2 등은 "기능(질문 내용·후보·최종 status)은 타당하나 type 라벨만 기대와 달라" FAIL 처리된 것으로 개발자가 트랜스크립트를 직접 검토해 확인함(v1-backlog #11). 즉 위 PASS/FAIL 수치는 "엄격한 라벨 일치" 기준이며 실제 기능 성공률은 이보다 높을 수 있음(정량 재측정 필요).

## 10. 대표적인 성공 사례 3개

| # | 케이스 | 입력 | 결과 | 의의 |
|---|---|---|---|---|
| 1 | R1 (08-18, 08-19 모두 PASS) | "이거 보고 준비 계획 짜줘." + 첨부: AI 아이디어 공모전 안내문 | `reference` "이거" → "AI 아이디어 공모전 안내문"으로 RESOLVED, 질문 0개, improved_prompt에 대상 명시 + 첨부문서 반영 지침 | 첨부 문서를 유일 후보로 잡아 질문 없이 명시화 — 핵심 시나리오 |
| 2 | 수동검증 Case B→C (08-13, Claude Desktop) | "그 기능 빼줘." + 대화(자동저장/알림 추가) → 답변 "자동 저장 기능" | 후보 2개 + impact high → 질문 생성 → 답변 이어받기로 decision을 RESOLVED 전환, `question_id` 유지, improved_prompt "자동 저장 기능 빼줘." | 세션 DB 없이 무상태 이어받기가 실제 클라이언트에서 동작 |
| 3 | QLIMIT1 (08-19 PASS) | "그거 전처럼 적당히 바꿔서 필요한 부분만 정리해줘." (문맥 없음) | 모호성 4종(reference/comparison/degree/scope) 동시 탐지되어도 질문 3개 이하로 압축 | 질문 개수 상한·우선순위 로직 검증 |

(추가 공통 PASS: O4 "바꿔줘."→질문, D3 강조만 있고 기준값 없는 degree→질문, CTX2 두 문서 마감일 충돌→질문)

## 11. 대표적인 실패/오류 사례 3개

| # | 케이스 | 기대 | 실제 | 원인 |
|---|---|---|---|---|
| 1 | O1·D1·D4·C1·M1·S4 (08-19 ERROR) | 정상 JSON 응답 | `qwen3.6-27b`가 `<think>` 안에서 "Wait, let me reconsider..."를 수천 단어 반복하다 `</think>`나 최종 JSON 출력 전 응답 절단 → `Unexpected end of JSON input` | `GroqLLMClient`에 `max_tokens` 미설정. reasoning 모델의 사고 길이가 완료 한도를 먼저 초과 (v1-backlog #5, P0) |
| 2 | D2 (08-19 FAIL) | "설명을 조금만 더 자세하게 써줘." → degree SAFE_INFERENCE, 질문 0 | "조금만 더"를 impact high로 판단 → CLARIFICATION_REQUIRED, "구체적인 기준이 있다면 알려주세요" 질문 | degree 특칙(강조+기준값 없음→high)이 강조 없는 일반 정도부사에 과잉 일반화 (v1-backlog #6) |
| 3 | FP1·FP2·C3 (08-19 FAIL) | "이 부분, 즉 결론 문단만 수정해줘." → 질문 없이 처리 | reference/scope는 "결론 문단"으로 RESOLVED했으나 "수정해줘"에 omission을 또 생성해 재질문 | omission 판정이 동사 어미만 보고, 같은 문장에서 대상이 이미 확정됐는지 확인 안 함 (v1-backlog #7) |

(추가: R2 — 4턴 대화 후 "이 부분"을 가장 최근 화제(성장과정)로 좁히지 못하고 질문. CTX1 — "이번에는 700자"라는 override 발화가 문서 1,000자보다 우선함을 인식 못 함.)

## 12~13. 오류를 해결하기 위해 수정한 내용 / 수정 전후 차이

| 항목 | 수정 전 | 수정 후 | 근거 파일 |
|---|---|---|---|
| reasoning 모델 `<think>` 혼입 | LLM 응답을 그대로 `JSON.parse` → reasoning 모델에서 파싱 실패 | `parseJsonResponse()`가 `<think>...</think>` 제거 후 코드펜스(```json) 추출, 그 다음 파싱. 회귀 테스트 4건 추가 | `src/llm/client.ts:55`, `test/llmClient.test.ts` |
| 평가 run 날짜 경계 유실 | 실행 시각 UTC 날짜로 결과 파일 생성 → 자정 넘기면 이전 진행분과 분리 | `resolveRunDate()`: 최신 `v0-additional-*.jsonl` 날짜 재사용, `EVAL_FRESH=1`일 때만 새 파일 | `scripts/evaluate-v0-additional.ts:43` |
| Gemini quota 대량 실패로 평가 중단 | 단발 호출, 실패 시 전체 run 실패 | 429/RESOURCE_EXHAUSTED 감지 시 20s/40s/70s backoff 재시도, 케이스별 즉시 JSONL append(crash-safe), `--report-only` 재리포트, 완료 케이스 skip. 이후 provider를 Groq로 교체해 재평가 | `scripts/evaluate-v0-additional.ts` (retry/persist 로직) |
| 모호성 최종 판정의 재현성 | (설계 결정) LLM이 decision까지 내면 실행마다 흔들림 | AmbiguityAnalyzer는 candidates/impact/context_support만, 최종 decision은 `decisionEngine`의 순수 함수 3규칙이 결정 | `src/decisionEngine.ts`, `src/ambiguityAnalyzer.ts` 주석 |

> v1-backlog에 정리됐으나 **아직 코드에 반영되지 않은** 개선안: Groq `max_tokens`/`reasoning_format` 설정, degree impact 조건 분리(강제성 신호 유무), omission 중복 억제 순서 규칙, 최근성/출처 우선순위 프롬프트 지침, zod 위반 시 1회 self-repair 재시도, 평가 채점을 "기능 지표 우선 + type 참고"로 완화.

## 14. 현재 시스템에서 확인할 수 있는 성과

- 자동 테스트 31개 전부 통과 — DecisionEngine 3규칙, 5유형 x 3경로, 답변 이어받기, JSON 파싱 방어, 프롬프트 조립이 결정론적으로 안정 동작
- 실제 LLM(Gemini, Groq) + 실제 MCP 클라이언트(Claude Desktop)에서 3개 처리 경로 + 이어받기가 일관 동작함을 2026-08-13에 확인
- 규칙/LLM 역할 분리로, LLM을 바꿔도(Gemini↔Groq) 파이프라인·테스트가 그대로 재사용됨을 2회 평가로 입증
- 30케이스 자동 평가 하네스로 실패를 8가지로 분류·재현 가능 → v1 개선 항목 12개를 근거 기반으로 도출
- "실제 모델의 모호성 감지가 사람의 stub 가정보다 보수적일 수 있다"는 관찰을 실측(예: RESOLVED 예상이 CLARIFICATION_REQUIRED로)

## 15. 아직 검증하지 못한 부분

- **정량 지표 전부 — 아직 정량평가하지 않음**: 정확도, 질문 정확률(precision/recall), 불필요한 질문 감소율, 모호성 해소율, 토큰 절감률, 응답 지연, 최종 LLM 출력 품질 개선도 등 어떤 수치도 측정·기록되지 않음. 평가 하네스의 PASS/FAIL은 라벨 엄격 일치 기준의 케이스 카운트일 뿐 성능 지표가 아님
- Anthropic(`claude-sonnet-4-5`) 경로 실행 검증
- Gemini `gemini-flash-latest`로 30케이스 완주 (2회 모두 quota로 중단)
- 안정적 provider(Groq max_tokens 등 수정 후)에서의 재평가
- planning Task 케이스의 충분한 커버리지 (fixture 대부분이 writing 중심)
- 실제 사용자 피드백 (경계 사례의 "적절한 신중함 vs 과한 질문" 판단)
- 다중 턴·다중 문서 문맥에서의 최근성/출처 우선순위
- Claude Desktop 연동 재검증 (2026-08-13 이후 없음)
- MCP Inspector를 통한 상세 점검 기록

## 16. 향후 진행해야 하는 실험

1. Groq `max_tokens`(4096 이상)·`reasoning_format`/`reasoning_effort` 설정 후 30케이스 재평가 → ERROR율 변화 측정
2. degree impact 조건 분리 프롬프트 보강 후 D2/S3/R2 회귀 확인
3. omission 중복 억제 규칙 추가 후 FP1/FP2/C3/S2 회귀 확인
4. 평가 채점 로직을 "핵심 기능 지표 우선"으로 개편하고, type 라벨 이슈와 실제 로직 버그 분리 재집계
5. **정량 평가 설계·수행**: (a) 유형별 decision 정확도, (b) 질문 발생 precision/recall(불필요 질문·질문 누락), (c) 명시화 성공률(improved_prompt에 모호 표현 잔존 여부), (d) 개선 프롬프트 사용 시 최종 LLM 출력 품질 A/B 비교, (e) 토큰 사용량 비교 — 각 항목에 대해 정답 라벨링된 데이터셋 구축
6. provider 교차 평가 (동일 fixture, Anthropic/Gemini/Groq) → 규칙-LLM 분리 설계의 provider 견고성 정량화
7. planning Task 전용 케이스 추가
8. 실사용자 대상 소규모 사용성 테스트 (경계 사례 질문 적절성)

---

## 부록. 확인 필요 항목 요약

- `.env`의 `LLM_PROVIDER` 실제 값 및 API key 값(비공개)
- Anthropic 경로가 실제로 1회라도 실행된 적 있는지 (기록 없음, `.env`에 `ANTHROPIC_API_KEY` 부재)
- 코드에서 참조하는 "스펙 3번/5번/7번/8번"의 원본 요구사항 문서(리포지토리에 없음 — 별도 관리 추정)
- 2026-08-13 이후 Claude Desktop 재검증 여부

---

## 부록. 정량 수치 표 (측정된 값만)

| 지표 | 값 | 비고 |
|---|---|---|
| 자동 단위 테스트 수 | 31 | 전부 PASS |
| 실LLM 평가 fixture 케이스 수 | 30 | 10개 카테고리 |
| 08-18 Gemini: PASS / FAIL / REVIEW / ERROR | 6 / 8 / 1 / 15 | 정상 완료 15, quota 실패 13 |
| 08-19 Groq: PASS / FAIL / REVIEW / ERROR | 9 / 12 / 2 / 7 | 정상 완료 23, JSON 파싱 실패 6 |
| 08-19 Gemini(재시도, archive): PASS / FAIL / ERROR | 0 / 2 / 28 | quota 실패 24, 유효 데이터 아님 |
| 수동 실연동 검증(08-13) 성공 | 3 / 3 | Case A/B/C |
| 정확도·토큰 절감률·성능 향상률 | **아직 정량평가하지 않음** | 측정 기록 없음 |
